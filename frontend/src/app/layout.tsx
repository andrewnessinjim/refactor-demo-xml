import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import * as motion from "motion/react-client";
import { slideUpAnimation } from "@/animations";
import Spacer from "@/components/Spacer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

const transparentToOpaqueAnimation = {
  initial: {
    opacity: 0,
  },
  animate: {
    opacity: 1,
  },
};

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-full">
      <motion.body
        className={`${geistSans.variable} ${geistMono.variable} antialiased h-full bg-gray-950`}
      >
        <motion.div {...transparentToOpaqueAnimation}>
          <motion.section
            {...slideUpAnimation}
            className="flex flex-col justify-center items-center"
          >
            <Spacer size="clamp(2.5rem, 10vw, 5rem)" />
            <h1 className="text-fluid-h1 text-center">Pet Enquiry</h1>
            <Spacer size="24px" />
            {children}
          </motion.section>
        </motion.div>
      </motion.body>
    </html>
  );
}
